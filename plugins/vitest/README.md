# vitest

Vitest TDD workflow and test writing conventions.

## Install

```text
/plugin install vitest@fabien-claude-marketplace
```

## Skills (2)

| Skill | Purpose |
|---|---|
| [`vitest-tdd-workflow`](skills/vitest-tdd-workflow/SKILL.md) | Cross-layer TDD iterations (Controller/Domain/Repository), working app at every GREEN, bug-fix-first-test, mocks-hiding-bugs pitfall |
| [`vitest-test-conventions`](skills/vitest-test-conventions/SKILL.md) | Vitest/NestJS test layers, `vi.fn()`/`vi.spyOn()`/`vi.mock()` patterns, `it.each()` format selection, structured assertions, factory functions, NestJS integration test setup |
