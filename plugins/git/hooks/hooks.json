{
  "description": "Fetch-first guard: block gh pr create / git push / git switch -c / git checkout -b when tracking refs are stale (FETCH_HEAD older than 10 min or absent) and a remote exists, so git decisions never run on perimed refs.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/fetch-first.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
