{
  "description": "Fetch-first guard: block branch creation (git switch -c / git checkout -b) when tracking refs are stale (FETCH_HEAD older than 10 min or absent) and a remote exists, so a branch is never cut from a lagging base. Pushing and gh pr create are deliberately not guarded — neither reads local refs, so no staleness can change what they do.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/fetch-first.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
